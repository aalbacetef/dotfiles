

## skhd 

the skhdrc file should be symlinked to ~/.skhdrc

## shell

1) install homebrew
2) install bash with homebrew 
3) add the homebrew bash path (usually /usr/local/bin/bash) to /etc/shells 
4) chsh -s /usr/local/bin/bash



